import 'package:flutter/material.dart';

class ListMenu {
  final String title;
  final IconData icon;
  final Color iconColor;
  final bool isNew;

  ListMenu({
    required this.title,
    required this.icon,
    required this.iconColor,
    required this.isNew,
  });
} 